{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "default",
  "description": "Grant the remote Arizona app (loaded via WebviewUrl::External) IPC access to the core window commands and event listening it needs. Tauri restricts IPC to bundled code by default, so a remote-URL page must be allowlisted here. Adjust `remote.urls` to your deployment's origin(s).",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:4040/*"]
  },
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-set-focus",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-set-fullscreen",
    "core:window:allow-set-content-protected"
  ]
}
