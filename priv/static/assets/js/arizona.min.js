/*! Arizona Framework Client v0.2.0 | Apache-2.0 License */
const Z = '/*! Arizona Framework Client v0.2.0 | Apache-2.0 License */(function(){"use strict";class a{constructor(){this.structure=new Map}initialize(t){this.structure=new Map(Object.entries(JSON.parse(JSON.stringify(t))))}mergeStructures(t){for(const[e,s]of Object.entries(t))console.log("[Arizona] Merging new structure into map:",e),this.structure.set(e,s)}applyDiff(t,e){if(e?.type==="stateful"){this.structure.set(e.id,e);return}if(!this.structure.has(t)){const r=String(t).replace(/\\r|\\n/g,"");console.warn(`[Arizona] StatefulId \'${r}\' not found in structure`)}const s=this.structure.get(t);console.log("[Arizona] Applying diff to",t),console.log("[Arizona] Changes:",e),console.log("[Arizona] Component dynamic before:",JSON.parse(JSON.stringify(s.dynamic)));for(const[r,i]of e)console.log("[Arizona] Applying change - index:",r,"value:",i),this.applyDiffValue(s.dynamic,r-1,i);console.log("[Arizona] Component dynamic after:",JSON.parse(JSON.stringify(s.dynamic)))}applyDiffValue(t,e,s){const r=t[e];if(r?.type==="stateful"){if(s?.type==="stateful"){console.log("[Arizona] Updating component reference:",s.id),t[e]=s;return}if(Array.isArray(s)){const i=this.structure.get(r.id);if(!i){const n=String(r.id).replace(/\\r|\\n/g,"");console.warn(`[Arizona] Component \'${n}\' not found in structure`);return}console.log("[Arizona] Applying nested diff to:",r.id),s.forEach(([n,h])=>{this.applyDiffValue(i.dynamic,n-1,h)});return}console.log("[Arizona] Removing component reference:",r.id),t[e]=s;return}if(s?.type){t[e]=s;return}if(Array.isArray(s)){if(r?.type==="list"){r.dynamic=s;return}if(r?.type==="stateless"){s.forEach(([i,n])=>{this.applyDiffValue(r.dynamic,i-1,n)});return}t[e]=s;return}t[e]=s}generateStatefulHTML(t){console.log("[Arizona] generateStatefulHTML for",t);const e=this.structure.get(t);if(!e){const r=String(t).replace(/\\r|\\n/g,"");throw console.warn(`[Arizona] StatefulId \'${r}\' not found in structure`),new Error(`Component ${r} not found`)}console.log("[Arizona] Structure found, generating HTML...");const s=this.zipStaticDynamic(e.static,e.dynamic);return console.log("[Arizona] HTML generated for",t,"length:",s.length),s}generateStatelessHTML(t){return this.zipStaticDynamic(t.static,t.dynamic)}generateListHTML(t){const{static:e,dynamic:s}=t;return s.reduce((r,i)=>r+this.zipStaticDynamic(e,i),"")}zipStaticDynamic(t,e){const s=[],r=Math.max(t.length,e.length);for(let i=0;i<r;i++)i<t.length&&s.push(t[i]),i<e.length&&s.push(this.normalizeDynamicElement(e[i]));return s.join("")}normalizeDynamicElement(t){return typeof t=="string"?t:t&&t.type==="stateful"?this.generateStatefulHTML(t.id):t&&t.type==="stateless"?this.generateStatelessHTML(t):t&&t.type==="list"?this.generateListHTML(t):Array.isArray(t)?this.flattenIoData(t):String(t)}flattenIoData(t){return typeof t=="string"?t:typeof t=="number"?String(t):Array.isArray(t)?t.map(e=>this.flattenIoData(e)).join(""):t&&typeof t=="object"?t.type==="stateful"?this.generateStatefulHTML(t.id):t.type==="stateless"?this.generateStatelessHTML(t):t.type==="list"?this.generateListHTML(t):String(t):String(t||"")}getStructure(){return JSON.parse(JSON.stringify(Object.fromEntries(this.structure)))}isInitialized(){return this.structure.size>0}getComponentIds(){return Array.from(this.structure.keys())}clear(){this.structure=new Map}createPatch(t){console.log("[Arizona] createPatch called for",t);const e=this.generateStatefulHTML(t);return console.log("[Arizona] HTML generated, length:",e.length),{type:"html_patch",statefulId:t,html:e}}}class o{constructor(){this.socket=null,this.connected=!1,this.messageQueue=[],this.hierarchical=new a,self.onmessage=t=>{const{type:e,data:s}=t.data;switch(e){case"connect":this.connect(s.url);break;case"send":this.sendMessage(s);break;case"disconnect":this.disconnect();break}}}connect(t){this.connected||(this.socket=new WebSocket(t),this.socket.onopen=()=>{this.connected=!0,this.postMessage({type:"status",data:{status:"connected"}}),this.flushMessageQueue()},this.socket.onmessage=e=>{const s=JSON.parse(e.data);this.handleWebSocketMessage(s)},this.socket.onclose=()=>{this.connected=!1,this.postMessage({type:"status",data:{status:"disconnected"}})},this.socket.onerror=e=>{this.postMessage({type:"error",data:{error:e.toString()}})})}sendMessage(t){const e=JSON.stringify(t);this.connected&&this.socket.readyState===WebSocket.OPEN?this.socket.send(e):this.messageQueue.push(e)}flushMessageQueue(){for(;this.messageQueue.length>0;){const t=this.messageQueue.shift();if(this.socket.readyState===WebSocket.OPEN)this.socket.send(t);else{this.messageQueue.unshift(t);break}}}disconnect(){this.socket&&(this.socket.close(),this.socket=null),this.connected=!1,this.hierarchical.clear()}handleWebSocketMessage(t){try{switch(t.type){case"initial_render":this.handleInitialRender(t);break;case"diff":this.handleDiff(t);break;case"reload":this.handleReload(t);break;case"dispatch":this.handleDispatch(t);break;case"reply":this.handleReply(t);break;case"redirect":this.handleRedirect(t);break;default:this.handleUnknownMessage(t)}}catch(e){this.postMessage({type:"error",data:{error:`Message handling failed: ${e.message}`}})}}handleInitialRender(t){this.hierarchical.initialize(t.structure)}handleDiff(t){if(!this.hierarchical.isInitialized())throw new Error("Hierarchical structure not initialized");t.structure&&Object.keys(t.structure).length>0&&(console.log("[Worker] Merging new structures:",Object.keys(t.structure)),this.hierarchical.mergeStructures(t.structure)),console.log("[Worker] Handling diff for",t.stateful_id),this.hierarchical.applyDiff(t.stateful_id,t.changes),console.log("[Worker] Creating patch for",t.stateful_id);const e=this.hierarchical.createPatch(t.stateful_id);console.log("[Worker] Patch created:",e),this.postMessage({type:"html_patch",data:{patch:e}}),console.log("[Worker] Patch sent to main thread")}handleReload(t){this.postMessage({type:"reload",data:t})}handleDispatch(t){this.postMessage({type:"dispatch",data:t})}handleReply(t){this.postMessage({type:"reply",data:t})}handleRedirect(t){this.postMessage({type:"redirect",data:{url:t.url,target:t.target}})}handleUnknownMessage(t){this.postMessage(t)}postMessage(t){self.postMessage(t)}}new o})();\n//# sourceMappingURL=arizona-worker.min.js.map\n', Q = typeof self < "u" && self.Blob && new Blob(["(self.URL || self.webkitURL).revokeObjectURL(self.location.href);", Z], { type: "text/javascript;charset=utf-8" });
function ae(s) {
  let e;
  try {
    if (e = Q && (self.URL || self.webkitURL).createObjectURL(Q), !e) throw "";
    const t = new Worker(e, {
      name: s?.name
    });
    return t.addEventListener("error", () => {
      (self.URL || self.webkitURL).revokeObjectURL(e);
    }), t;
  } catch {
    return new Worker(
      "data:text/javascript;charset=utf-8," + encodeURIComponent(Z),
      {
        name: s?.name
      }
    );
  }
}
var G = 11;
function oe(s, e) {
  var t = e.attributes, r, n, l, h, g;
  if (!(e.nodeType === G || s.nodeType === G)) {
    for (var p = t.length - 1; p >= 0; p--)
      r = t[p], n = r.name, l = r.namespaceURI, h = r.value, l ? (n = r.localName || n, g = s.getAttributeNS(l, n), g !== h && (r.prefix === "xmlns" && (n = r.name), s.setAttributeNS(l, n, h))) : (g = s.getAttribute(n), g !== h && s.setAttribute(n, h));
    for (var b = s.attributes, M = b.length - 1; M >= 0; M--)
      r = b[M], n = r.name, l = r.namespaceURI, l ? (n = r.localName || n, e.hasAttributeNS(l, n) || s.removeAttributeNS(l, n)) : e.hasAttribute(n) || s.removeAttribute(n);
  }
}
var H, le = "http://www.w3.org/1999/xhtml", f = typeof document > "u" ? void 0 : document, ce = !!f && "content" in f.createElement("template"), he = !!f && f.createRange && "createContextualFragment" in f.createRange();
function de(s) {
  var e = f.createElement("template");
  return e.innerHTML = s, e.content.childNodes[0];
}
function ue(s) {
  H || (H = f.createRange(), H.selectNode(f.body));
  var e = H.createContextualFragment(s);
  return e.childNodes[0];
}
function fe(s) {
  var e = f.createElement("body");
  return e.innerHTML = s, e.childNodes[0];
}
function ge(s) {
  return s = s.trim(), ce ? de(s) : he ? ue(s) : fe(s);
}
function _(s, e) {
  var t = s.nodeName, r = e.nodeName, n, l;
  return t === r ? !0 : (n = t.charCodeAt(0), l = r.charCodeAt(0), n <= 90 && l >= 97 ? t === r.toUpperCase() : l <= 90 && n >= 97 ? r === t.toUpperCase() : !1);
}
function pe(s, e) {
  return !e || e === le ? f.createElement(s) : f.createElementNS(e, s);
}
function ye(s, e) {
  for (var t = s.firstChild; t; ) {
    var r = t.nextSibling;
    e.appendChild(t), t = r;
  }
  return e;
}
function j(s, e, t) {
  s[t] !== e[t] && (s[t] = e[t], s[t] ? s.setAttribute(t, "") : s.removeAttribute(t));
}
var K = {
  OPTION: function(s, e) {
    var t = s.parentNode;
    if (t) {
      var r = t.nodeName.toUpperCase();
      r === "OPTGROUP" && (t = t.parentNode, r = t && t.nodeName.toUpperCase()), r === "SELECT" && !t.hasAttribute("multiple") && (s.hasAttribute("selected") && !e.selected && (s.setAttribute("selected", "selected"), s.removeAttribute("selected")), t.selectedIndex = -1);
    }
    j(s, e, "selected");
  },
  /**
   * The "value" attribute is special for the <input> element since it sets
   * the initial value. Changing the "value" attribute without changing the
   * "value" property will have no effect since it is only used to the set the
   * initial value.  Similar for the "checked" attribute, and "disabled".
   */
  INPUT: function(s, e) {
    j(s, e, "checked"), j(s, e, "disabled"), s.value !== e.value && (s.value = e.value), e.hasAttribute("value") || s.removeAttribute("value");
  },
  TEXTAREA: function(s, e) {
    var t = e.value;
    s.value !== t && (s.value = t);
    var r = s.firstChild;
    if (r) {
      var n = r.nodeValue;
      if (n == t || !t && n == s.placeholder)
        return;
      r.nodeValue = t;
    }
  },
  SELECT: function(s, e) {
    if (!e.hasAttribute("multiple")) {
      for (var t = -1, r = 0, n = s.firstChild, l, h; n; )
        if (h = n.nodeName && n.nodeName.toUpperCase(), h === "OPTGROUP")
          l = n, n = l.firstChild, n || (n = l.nextSibling, l = null);
        else {
          if (h === "OPTION") {
            if (n.hasAttribute("selected")) {
              t = r;
              break;
            }
            r++;
          }
          n = n.nextSibling, !n && l && (n = l.nextSibling, l = null);
        }
      s.selectedIndex = t;
    }
  }
}, T = 1, X = 11, q = 3, Y = 8;
function w() {
}
function ve(s) {
  if (s)
    return s.getAttribute && s.getAttribute("id") || s.id;
}
function me(s) {
  return function(t, r, n) {
    if (n || (n = {}), typeof r == "string")
      if (t.nodeName === "#document" || t.nodeName === "HTML" || t.nodeName === "BODY") {
        var l = r;
        r = f.createElement("html"), r.innerHTML = l;
      } else
        r = ge(r);
    else r.nodeType === X && (r = r.firstElementChild);
    var h = n.getNodeKey || ve, g = n.onBeforeNodeAdded || w, p = n.onNodeAdded || w, b = n.onBeforeElUpdated || w, M = n.onElUpdated || w, ee = n.onBeforeNodeDiscarded || w, L = n.onNodeDiscarded || w, te = n.onBeforeElChildrenUpdated || w, re = n.skipFromChildren || w, V = n.addChild || function(i, a) {
      return i.appendChild(a);
    }, E = n.childrenOnly === !0, k = /* @__PURE__ */ Object.create(null), R = [];
    function z(i) {
      R.push(i);
    }
    function B(i, a) {
      if (i.nodeType === T)
        for (var d = i.firstChild; d; ) {
          var o = void 0;
          a && (o = h(d)) ? z(o) : (L(d), d.firstChild && B(d, a)), d = d.nextSibling;
        }
    }
    function O(i, a, d) {
      ee(i) !== !1 && (a && a.removeChild(i), L(i), B(i, d));
    }
    function P(i) {
      if (i.nodeType === T || i.nodeType === X)
        for (var a = i.firstChild; a; ) {
          var d = h(a);
          d && (k[d] = a), P(a), a = a.nextSibling;
        }
    }
    P(t);
    function I(i) {
      p(i);
      for (var a = i.firstChild; a; ) {
        var d = a.nextSibling, o = h(a);
        if (o) {
          var c = k[o];
          c && _(a, c) ? (a.parentNode.replaceChild(c, a), U(c, a)) : I(a);
        } else
          I(a);
        a = d;
      }
    }
    function ne(i, a, d) {
      for (; a; ) {
        var o = a.nextSibling;
        (d = h(a)) ? z(d) : O(
          a,
          i,
          !0
          /* skip keyed nodes */
        ), a = o;
      }
    }
    function U(i, a, d) {
      var o = h(a);
      if (o && delete k[o], !d) {
        var c = b(i, a);
        if (c === !1 || (c instanceof HTMLElement && (i = c, P(i)), s(i, a), M(i), te(i, a) === !1))
          return;
      }
      i.nodeName !== "TEXTAREA" ? se(i, a) : K.TEXTAREA(i, a);
    }
    function se(i, a) {
      var d = re(i, a), o = a.firstChild, c = i.firstChild, S, y, A, N, v;
      e: for (; o; ) {
        for (N = o.nextSibling, S = h(o); !d && c; ) {
          if (A = c.nextSibling, o.isSameNode && o.isSameNode(c)) {
            o = N, c = A;
            continue e;
          }
          y = h(c);
          var C = c.nodeType, m = void 0;
          if (C === o.nodeType && (C === T ? (S ? S !== y && ((v = k[S]) ? A === v ? m = !1 : (i.insertBefore(v, c), y ? z(y) : O(
            c,
            i,
            !0
            /* skip keyed nodes */
          ), c = v, y = h(c)) : m = !1) : y && (m = !1), m = m !== !1 && _(c, o), m && U(c, o)) : (C === q || C == Y) && (m = !0, c.nodeValue !== o.nodeValue && (c.nodeValue = o.nodeValue))), m) {
            o = N, c = A;
            continue e;
          }
          y ? z(y) : O(
            c,
            i,
            !0
            /* skip keyed nodes */
          ), c = A;
        }
        if (S && (v = k[S]) && _(v, o))
          d || V(i, v), U(v, o);
        else {
          var W = g(o);
          W !== !1 && (W && (o = W), o.actualize && (o = o.actualize(i.ownerDocument || f)), V(i, o), I(o));
        }
        o = N, c = A;
      }
      ne(i, c, y);
      var J = K[i.nodeName];
      J && J(i, a);
    }
    var u = t, D = u.nodeType, F = r.nodeType;
    if (!E) {
      if (D === T)
        F === T ? _(t, r) || (L(t), u = ye(t, pe(r.nodeName, r.namespaceURI))) : u = r;
      else if (D === q || D === Y) {
        if (F === D)
          return u.nodeValue !== r.nodeValue && (u.nodeValue = r.nodeValue), u;
        u = r;
      }
    }
    if (u === r)
      L(t);
    else {
      if (r.isSameNode && r.isSameNode(u))
        return;
      if (U(u, r, E), R)
        for (var x = 0, ie = R.length; x < ie; x++) {
          var $ = k[R[x]];
          $ && O($, $.parentNode, !1);
        }
    }
    return !E && u !== t && t.parentNode && (u.actualize && (u = u.actualize(t.ownerDocument || f)), t.parentNode.replaceChild(u, t)), u;
  };
}
var we = me(oe);
class be {
  /**
   * Creates a new Arizona client instance
   * @param {ArizonaOptions} [opts={}] - Client configuration options
   */
  constructor(e = {}) {
    this.worker = null, this.connected = !1, this.eventListeners = /* @__PURE__ */ new Map(), this.logger = e.logger || null, this.nextRefId = 0, this.pendingCalls = /* @__PURE__ */ new Map();
  }
  /**
   * Initialize worker if not already created
   * @private
   * @returns {void}
   */
  initializeWorker() {
    this.worker || (this.worker = new ae(), this.worker.onmessage = (e) => {
      this.handleWorkerMessage(e.data);
    });
  }
  /**
   * Connect to the Arizona WebSocket server
   * @param {string} websocketEndpoint - WebSocket endpoint path
   * @returns {void}
   */
  connect(e) {
    if (this.connected) return;
    this.initializeWorker();
    const t = window.location.protocol === "https:" ? "wss:" : "ws:", r = window.location.host, n = window.location.pathname, l = window.location.search, h = encodeURIComponent(n), g = l ? encodeURIComponent(l.substring(1)) : "", p = `${t}//${r}${e}?path=${h}&qs=${g}`;
    this.worker.postMessage({
      type: "connect",
      data: { url: p }
    });
  }
  /**
   * Push an event to the Arizona server
   * @param {string} event - Event name
   * @param {EventParams} [params={}] - Event parameters
   * @returns {void}
   */
  pushEvent(e, t = {}) {
    this.connected && this.worker.postMessage({
      type: "send",
      data: {
        type: "event",
        event: e,
        params: t
      }
    });
  }
  /**
   * Push an event to a specific stateful component
   * @param {string} statefulId - Target stateful component ID
   * @param {string} event - Event name
   * @param {EventParams} [params={}] - Event parameters
   * @returns {void}
   */
  pushEventTo(e, t, r = {}) {
    this.connected && this.worker.postMessage({
      type: "send",
      data: {
        type: "event",
        stateful_id: e,
        event: t,
        params: r
      }
    });
  }
  /**
   * Call an event on the Arizona server and wait for reply
   * @param {string} event - Event name
   * @param {EventParams} [params={}] - Event parameters
   * @param {Object} [options={}] - Call options
   * @param {number} [options.timeout=10000] - Timeout in milliseconds
   * @returns {Promise<*>} Promise that resolves with reply data
   */
  callEvent(e, t = {}, r = {}) {
    return this._callEvent(void 0, e, t, r);
  }
  /**
   * Call an event on a specific stateful component and wait for reply
   * @param {string} statefulId - Target stateful component ID
   * @param {string} event - Event name
   * @param {EventParams} [params={}] - Event parameters
   * @param {Object} [options={}] - Call options
   * @param {number} [options.timeout=10000] - Timeout in milliseconds
   * @returns {Promise<*>} Promise that resolves with reply data
   */
  callEventFrom(e, t, r = {}, n = {}) {
    return this._callEvent(e, t, r, n);
  }
  /**
   * Internal helper to call an event and wait for reply
   * @private
   * @param {string|undefined} statefulId - Target stateful component ID (undefined for view)
   * @param {string} event - Event name
   * @param {EventParams} params - Event parameters
   * @param {Object} options - Call options
   * @returns {Promise<*>} Promise that resolves with reply data
   */
  _callEvent(e, t, r, n) {
    if (!this.connected) return Promise.reject(new Error("Not connected"));
    const l = `${++this.nextRefId}`;
    return new Promise((h, g) => {
      const p = setTimeout(() => {
        this.pendingCalls.delete(l), g(new Error(`Call timeout: ${t}`));
      }, n.timeout || 1e4);
      this.pendingCalls.set(l, { resolve: h, reject: g, timeout: p });
      const b = {
        type: "event",
        ref_id: l,
        event: t,
        params: r
      };
      e !== void 0 && (b.stateful_id = e), this.worker.postMessage({
        type: "send",
        data: b
      });
    });
  }
  /**
   * Disconnect from the Arizona WebSocket server
   * @returns {void}
   */
  disconnect() {
    this.worker && (this.worker.terminate(), this.worker = null), this.connected = !1, this.pendingCalls.forEach((e) => {
      clearTimeout(e.timeout), e.reject(new Error("Disconnected"));
    }), this.pendingCalls.clear();
  }
  /**
   * Handle messages from the worker thread
   * @private
   * @param {Object} message - Worker message
   * @returns {void}
   */
  handleWorkerMessage(e) {
    const { type: t, data: r } = e;
    try {
      switch (t) {
        case "status":
          this.handleStatus(r);
          break;
        case "initial_render":
          break;
        case "html_patch":
          this.handleHtmlPatch(r);
          break;
        case "error":
          this.handleWorkerError(r);
          break;
        case "reload":
          this.handleReload(r);
          break;
        case "dispatch":
          this.handleDispatch(r);
          break;
        case "reply":
          this.handleReply(r);
          break;
        case "redirect":
          this.handleRedirect(r);
          break;
        default:
          this.handleUnknownMessage(e);
      }
    } catch (n) {
      this.logger?.error("Error handling worker message:", n);
    }
  }
  handleStatus(e) {
    e.status === "connected" ? (this.connected = !0, this.logger?.info("Connected to WebSocket"), this.emit("connected", e)) : e.status === "disconnected" && (this.connected = !1, this.logger?.info("Disconnected from WebSocket"), this.emit("disconnected", e));
  }
  handleHtmlPatch(e) {
    console.log("[Arizona] Received HTML patch:", e.patch), this.logger?.debug("Applying HTML patch"), this.applyHtmlPatch(e.patch);
  }
  applyHtmlPatch(e) {
    console.log("[Arizona] Applying HTML patch to", e.statefulId);
    const t = document.getElementById(e.statefulId);
    if (!t) {
      console.warn("[Arizona] Target element not found:", e.statefulId), this.logger?.warning(`Target element not found: ${e.statefulId}`);
      return;
    }
    console.log(
      "[Arizona] Target found, morphing with HTML:",
      `${e.html.substring(0, 100)}...`
    );
    try {
      we(t, e.html, {
        onBeforeElUpdated(r, n) {
          return n.dataset?.arizonaUpdate === "false" ? !1 : !r.isEqualNode(n);
        }
      }), this.logger?.debug("Patch applied successfully");
    } catch (r) {
      this.logger?.error("Error applying HTML patch:", r);
    }
  }
  handleWorkerError(e) {
    this.logger?.error("Worker Error:", e.error), this.emit("error", e);
  }
  handleReload(e) {
    switch (e.file_type) {
      case "css":
        this.logger?.info("CSS file changed. Refreshing stylesheets without page reload..."), document.querySelectorAll('link[rel="stylesheet"]').forEach((t) => {
          const r = t.href.split("?")[0];
          t.href = `${r}?t=${Date.now()}`;
        });
        break;
      default:
        this.logger?.info(`${e.file_type || "File"} changed. Reloading page...`), window.location.reload();
    }
  }
  handleDispatch(e) {
    this.logger?.debug("Dispatching event:", e.event), this.emit(e.event, e.data);
  }
  handleReply(e) {
    const { ref_id: t, data: r } = e, n = this.pendingCalls.get(t);
    n ? (clearTimeout(n.timeout), n.resolve(r), this.pendingCalls.delete(t), this.logger?.debug(`Reply received for ref: ${t}`)) : this.logger?.warning(`Received reply for unknown ref: ${t}`);
  }
  handleRedirect(e) {
    this.logger?.info("Redirecting to:", e.url), window.open(e.url, e.options?.target, e.options?.window_features);
  }
  handleUnknownMessage(e) {
    this.logger?.warning("Unknown worker message:", e);
  }
  /**
   * Check if client is connected to server
   * @returns {boolean} True if connected
   */
  isConnected() {
    return this.connected;
  }
  /**
   * Subscribe to an Arizona event
   * @param {string} event - Event name (e.g., 'connected', 'disconnected')
   * @param {Function} callback - Callback function to invoke when event occurs
   * @returns {Function} Unsubscribe function
   */
  on(e, t) {
    return typeof t != "function" ? (this.logger?.error(`on: callback must be a function, got ${typeof t}`), () => {
    }) : (this.eventListeners.has(e) || this.eventListeners.set(e, /* @__PURE__ */ new Set()), this.eventListeners.get(e).add(t), this.logger?.debug(`Subscribed to event: ${e}`), () => this.off(e, t));
  }
  /**
   * Subscribe to an Arizona event that will only fire once
   * @param {string} event - Event name
   * @param {Function} callback - Callback function to invoke when event occurs
   * @returns {Function} Unsubscribe function
   */
  once(e, t) {
    if (typeof t != "function")
      return this.logger?.error(`once: callback must be a function, got ${typeof t}`), () => {
      };
    const r = (n) => {
      t(n), this.off(e, r);
    };
    return this.on(e, r);
  }
  /**
   * Unsubscribe from an Arizona event
   * @param {string} event - Event name
   * @param {Function} callback - Callback function to remove
   * @returns {void}
   */
  off(e, t) {
    const r = this.eventListeners.get(e);
    r && (r.delete(t), this.logger?.debug(`Unsubscribed from event: ${e}`), r.size === 0 && this.eventListeners.delete(e));
  }
  /**
   * Remove all listeners for a specific event, or all events if no event specified
   * @param {string} [event] - Optional event name. If not provided, removes all listeners for all events
   * @returns {void}
   */
  removeAllListeners(e) {
    e ? (this.eventListeners.delete(e), this.logger?.debug(`Removed all listeners for event: ${e}`)) : (this.eventListeners.clear(), this.logger?.debug("Removed all event listeners"));
  }
  /**
   * Emit an Arizona event to all subscribed listeners
   * @private
   * @param {string} event - Event name
   * @param {*} data - Event data to pass to listeners
   * @returns {void}
   */
  emit(e, t) {
    const r = this.eventListeners.get(e);
    r && r.forEach((n) => {
      try {
        n(t);
      } catch (l) {
        this.logger?.error(`Error in event listener for '${e}':`, l);
      }
    });
  }
}
export {
  be as default
};
//# sourceMappingURL=arizona.min.js.map
